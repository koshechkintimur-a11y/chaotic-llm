# SCIENCE_AUDIT.md — научная добросовестность прогона v2

Честный аудит того, **что именно** заявляется и **каким лежащим в репо артефактом**
это подтверждено. Цель — чтобы любой рецензент мог проверить каждое число до байта.

Все числа ниже — результат `phase01/exp_vq/final_benchmark_v2.py`, записаны в
`results/final_benchmark_v2.json` (агрегат, собранный из персидовых файлов
`results/bench_v2/*.json`). Прогон воспроизводим вне этой машины на публичном корпусе.

---

## 1. Заявление и доказательство

**Заявление.** Архитектура STS-Prog (Sparse Temporal Selection + Progressive refinement
поверх PurePCLM с PC-синхронизацией) при сопоставимом/меньшем бюджете параметров
обгоняет параметр-матч Transformer по перплексии и по induction-retrieval.

**Доказательство (v2, публичный корпус, 5 сидов, 6000 шагов, 95% CI):**

| Модель | Параметры | PPL mean±std | 95% CI (t) | Retr L16→L256 (pooled) |
|---|---|---|---|---|
| **STS-Prog** | 900 353 | 24.525 ± 0.352 | [24.12, 24.93] | 49.9 / 51.9 / 41.3 / 42.0 / 37.9 % |
| STS-Prog (no-PC) | 900 353 | 46.483 ± 0.975 | [45.36, 47.60] | 17.3 / 15.3 / 16.7 / 16.0 / 15.2 % |
| Transformer (D=88) | 865 904 | 49.132 ± 1.526 | [47.38, 50.89] | 20.3 / 20.5 / 18.1 / 16.3 / 16.9 % |
| Transformer-matched (D=92) | 940 568 | 46.620 ± 1.492 | [44.91, 48.33] | 21.8 / 20.7 / 19.4 / 16.1 / 15.8 % |

**Выводы, которые держатся:**
- STS-Prog (900K) бьёт Transformer-matched (941K, **больше** на 4.4%) по PPL в **1.9×**
  (24.5 против 46.6). Сравнение *консервативное*: победа при бОльшем бюджете оппонента.
- Абляция `no-PC` показывает, что выигрыш — от **хаоса/PC-синхронизации**, а не от
  архитектуры как таковой: отключение PC поднимает PPL с 24.5 до 46.5 (×1.9) и обрушивает
  retrieval с ~45% до ~16%. То есть retrieval-преимущество STS целиком от PC-синхронизации.
- Доверительные интервалы не перекрываются: STS-Prog CI [24.1, 24.9] лежит далеко от
  любого Transformer CI (нижняя граница TF ~44.9). Различие статистически значимо (n=5, t).

---

## 2. Три дыры v1 и как их закрыл v2

| # | Дыра в v1 | Как закрыто в v2 |
|---|---|---|
| (a) | `final_benchmark.py` читал приватный `corpus_train.txt` (в `.gitignore`, утечка исходников) — результаты невоспроизводимы | `prepare_public_corpus.py` качает **TheAlgorithms/Python** (MIT, pinned SHA `9391f546d6f8`) → `phase01/corpus_public.txt`; v2 идёт на нём. Тот же бюджет 990K символов и BPE-512, что у v1. |
| (b) | Параметр-матч был нечестным: STS (900 353) имел **+4%** к Transformer (865 904), «matched» брал меньшую модель | matched-Transformer = **D=92 (~941K) ≥ STS 900K** (см. `match_transformer.py` + цикл подбора в `final_benchmark_v2.py`). Победа STS — при бОльшем бюджете оппонента. |
| (c) | Retrieval-тест засевался сидом модели (`np.random.seed(seed)` внутри `train_model`), поэтому тестовая выборка менялась по сидам → разброс = смесь дисперсии модели и выборки | Retrieval считается на **одной фиксированной выборке** (`eval_seed=12345`) независимо от сида модели → across-seed разброс = чисто дисперсия модели. Плюс 5 сидов и **95% CI** (t-распределение). |

> Остальные дыры из `README.md` (TODO #1, #4) были закрыты ранее (v1-прогоном и
> фиксацией `k_init=1.2`). Дыры **#2 и #3** (ночной прогон на Stack: несогласованная
> глубина 12 vs 8, незаписанный знаменатель retrieval) **удалены из описания победителя
> как невалидированный legacy-эксперимент** — их числа (25.65 / 67% / 60%) не заявляются,
> артефакты сохранены в `phase01/exp_vq/` и вытеснены строгим v2-прогоном.

---

## 3. Методология (почему числа надёжны)

- **Инкрементальная запись + чекпоинты.** Харнесс пишет агрегат после КАЖДОГО сида и
  сохраняет `.pt`; падение не теряет прогоны; докачка пропускает готовые сиды.
- **Докачка защищена персидовыми файлами.** Агрегат `final_benchmark_v2.json` в этом
  прогоне формировался ДВУМЯ параллельными процессами (исходный `MDDCWp` и докачка `xVxAuk`,
  оба на публичном корпусе). Чтобы исключить любую порчу от параллельной записи в один файл,
  финальный агрегат **пересобран скриптом `rebuild_from_perseed.py` исключительно из
  атомарных персидовых файлов `results/bench_v2/*.json`** (каждый пишется отдельно на сид).
  `_meta` и `_scaling` перенесены из того же агрегата. Таким образом каждое число в таблице
  — это прямое чтение из файла конкретного сида, а не результат слияния.
- **Retrieval = pooled.** Доля верных предсказаний B в паттерне A→B, усреднённая по всем
  принятым пробам: сумма hits / сумма trials по всем сидам (знаменатель записан в JSON
  как `retrieval[L].trials`). 300 проб/дистанция на сид, 5 сидов → ≥1500 проб/дистанция.
- **PPL.** На тестовой выборке (последние 20% корпуса), batch=64, `exp(mean NLL)`.
- **Устройство.** CUDA (`torch 2.11.0+cu128`), mixed precision (GradScaler + autocast) на
  GPU, float32 на CPU. Для сравнения с v1 держите GPU.

---

## 4. Как воспроизвести (до байта)

```bash
cd phase01/exp_vq

# 1. Публичный корпус (MIT, воспроизводимый; ~990K символов)
py -3.13 prepare_public_corpus.py        # -> ../../phase01/corpus_public.txt + .SOURCE.md

# 2. Строгий прогон (5 сидов, фикс. retrieval-сид, консервативный параметр-матч)
py -3.13 final_benchmark_v2.py --device cuda --seeds 0,1,2,3,4 --corpus ../../phase01/corpus_public.txt
#    пишет results/final_benchmark_v2.json (инкрементально)

# 3. (опционально) пересобрать агрегат из персидовых файлов, если прогон шёл параллельно
py -3.13 rebuild_from_perseed.py

# 4. Влить таблицы в README
py -3.13 render_docs_v2.py --inject ../../README.md
```

Контрольная сумма утверждений: при тех же сидах и корпусе должны получиться
PPL STS-Prog ≈ 24.5 (CI [24.1, 24.9]) и retrieval L16 ≈ 49.9%. Расхождения в пределах
ширины 95% CI считаются нормой (дисперсия модели).

---

## 5. Что НЕ заявляется

- Не заявляется превосходство над Transformer на **длинных контекстах** (W≥1024) или при
  **100M+ параметров** — это долгосрочные цели, отдельные прогоны.
- Ночной прогон на Stack (бывшие дыры #2/#3) **исключён из заявлений** как невалидированный
  legacy-эксперимент: его числа (25.65 / 67% / 60%) не заявляются; артефакты сохранены в
  `phase01/exp_vq/`, но вытеснены строгим v2-прогоном на публичном корпусе.
- Числа получены на **code-корпусе** (TheAlgorithms/Python). Качество на естественном
  языке/диалоге отдельно не измерено в этом прогоне.
